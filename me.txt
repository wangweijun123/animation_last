总结就是:

视图动图:
属性动画: ObjectAnimator, 动画没有改变 mTop, mLeft, 改变的是 RenderNode


try {

}

try { 从哪行开始, 到哪行结束}
exception table (异常表) { exception }
如果都没有catch, 抛给虚拟机 --> 虚拟机c++调用 java层
Thread.dispatchUncaughtException{
    handler.uncaughtException
}